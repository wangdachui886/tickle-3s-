# 设计说明

这个目录只放适合公开的设计说明。

过程稿默认不进公开仓库，包括：

- Illustrator 源文件
- 小组件提案板
- 历史重绘稿
- 评审截图和 UI dump

App 正在使用的运行时资源在：

```text
app/src/main/res/
```

README 里展示用的截图和教程图在：

```text
docs/screenshots/
```

Logo 等最终公开素材在：

```text
docs/brand/
```

后面如果有新的商店截图或品牌图，先导出成最终 PNG / SVG，再放到 `docs/` 下；源文件继续留在本地。
